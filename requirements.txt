# Drakehaven Island itself has NO dependencies — it is static files, and the
# game runs from index.html with nothing installed.
#
# gunicorn is here only for hosts whose Python start command names it, e.g.
# `gunicorn app:app`. gunicorn.conf.py binds it to $PORT automatically.
# If your start command is `python server.py` or `python app.py`, or you deploy
# as a static site, nothing in this file is needed.
gunicorn>=21.2
